<Window x:Class="AutofacPresentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"        
        xmlns:frontend="clr-namespace:AutofacPresentation.Frontend"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" d:DataContext="{d:DesignInstance frontend:MainWindowViewModel}">
    <Grid>
        <frontend:HeaderView DataContext="{Binding Path=Header}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10"/>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
            <Button Content="Say Hello" Margin="10"/>
            <Button Content="Say Goodbye" Margin="10"/>
            <Button Content="Do calculate" Command="{Binding Path=RelayCommand}" Margin="10"/>
            <TextBlock Text="{Binding Path=ServiceResult}" Margin="10" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>